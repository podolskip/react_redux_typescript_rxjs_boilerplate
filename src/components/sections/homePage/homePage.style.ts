import { style } from 'typestyle';

export const homeCl = {
  jsonStyling: style({
    textAlign: 'left',
  }),
  jsonWrapper: style({
    textAlign: 'center',
  }),
  divTable: style({
    display: 'table',
    width: '100%',
  }),
  divInlineBlock: style({
    display: 'inline-block',
  }),
};